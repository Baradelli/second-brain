import React from 'react';
import ReactDOM from 'react-dom/client';

// Placeholder. Shell/layout mobile-first; importa o grosso de @cerebro/ui e @cerebro/shared.
// Setup real na Tarefa 19; telas nas Tarefas 20–23.
function App() {
  return <h1>Segundo Cérebro — mobile</h1>;
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
